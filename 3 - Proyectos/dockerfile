Crear un volumen de Docker:

    Puedes crear un volumen en Docker para almacenar los datos de OpenLDAP. Usa el siguiente comando para crear un volumen llamado ldap_data:

docker volume create ldap_data
